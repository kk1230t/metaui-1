
mixin button(params={})
  - const { elememt = 'button', size, labelText, isIcon, ...rest } = Object.assign({}, params)
  - const prefix = 'mui'
  #{elememt}.mui-button(
    class=`${prefix}-size-${size}`
    class={
      'mui-icon':isIcon,
    }
  )
    span
      | #{labelText}

